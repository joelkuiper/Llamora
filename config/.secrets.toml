[default]
# Add secrets (API keys, passwords) here. This file is loaded after
# ``settings.toml`` and should not be committed with real credentials.
#
# Example: external OpenAI-compatible API key
# [default.LLM.chat]
# api_key = "sk-..."
